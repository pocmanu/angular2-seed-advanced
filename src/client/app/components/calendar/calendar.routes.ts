import { RouterConfig } from '@angular/router';

import { CalendarComponent } from './calendar.component';

export const CalendarRoutes: RouterConfig = [
  {
    path: 'calendar',
    component: CalendarComponent
  }
];
