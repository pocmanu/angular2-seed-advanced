import { RouterConfig } from '@angular/router';

import { CounterComponent } from './counter.component';

export const CounterRoutes: RouterConfig = [
  {
    path: 'counter',
    component: CounterComponent
  }
];
